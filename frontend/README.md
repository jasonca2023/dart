# Dart — Frontend

> **Owned by the frontend agent. Work on the `frontend` branch, only under `frontend/`.**
> See [`../AGENTS.md`](../AGENTS.md) and the API spec in [`../docs/API_CONTRACT.md`](../docs/API_CONTRACT.md).

Next.js + TypeScript + Tailwind dashboard: **launch campaign → live status → review → export**.

## Scope
- Build against the shapes in `docs/API_CONTRACT.md`. **Mock** responses until the backend
  is live, then switch the base URL to `NEXT_PUBLIC_API_BASE_URL`.
- No provider secrets here — only `NEXT_PUBLIC_*` values reach the browser.

## Getting started
_To be filled in by the frontend agent (e.g. `npm run dev`)._
