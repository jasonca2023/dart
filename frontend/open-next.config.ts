import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// Default OpenNext config — runs the Next.js app as a Cloudflare Worker.
// Add caching/queue overrides here later if needed.
export default defineCloudflareConfig();
